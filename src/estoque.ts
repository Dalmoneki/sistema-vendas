class Estoque {
    produtoId: number;
    quantidade: number;
  
    constructor(produtoId: number, quantidade: number) {
      this.produtoId = produtoId;
      this.quantidade = quantidade;
    }
  }
  
  export default Estoque;
  